# API Profile

This profile uses:

- Rails API-only mode
- Grape for endpoint definition
- `grape-swagger` and `grape-swagger-rails` for docs
- `grape-entity` for response presentation

Swagger UI is available only when `SWAGGER_ENABLED=true`.
